# Examples

The sample CSV files in this directory are synthetic and are only intended to demonstrate the local-file workflow.

Generate them with:

```bash
python examples/make_synthetic_sample.py
```

For real market research, provide your own licensed ETF OHLCV data using the `market_prices.csv` schema described in the repository README.
